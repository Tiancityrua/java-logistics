<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="freight.dao.bill.de_bill_dao">
    <delete id="de_mawb" parameterType="java.util.Map">
        DELETE * FROM mawb WHERE mawb=#{mawb}
    </delete>
    <delete id="de_hawb" parameterType="java.util.Map">
        DELETE * FROM hawb WHERE hawb=#{hawb}
    </delete>
</mapper>